<Window x:Class="PaperbellAppDotNet.SyncLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shopee Sync" Height="520" Width="820"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtTitle" FontSize="16" FontWeight="SemiBold" Text="Syncing Shopee orders..." />

        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ProgressBar x:Name="Bar" Height="18" Minimum="0" Maximum="100" Value="0"/>

            <TextBlock x:Name="TxtProgress" Grid.Column="1" Margin="10,0,0,0"
                       VerticalAlignment="Center" Text="0%" />
        </Grid>

        <TextBox x:Name="LogBox" Grid.Row="2"
                 FontFamily="Consolas"
                 FontSize="12"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
			<Button x:Name="BtnClose" Content="Cancel" Width="90" IsEnabled="True" Click="BtnClose_Click"/>
		</StackPanel>
    </Grid>
</Window>
