<Window x:Class="PaperbellAppDotNet.OrderPackDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detail order"
        Height="480"
        Width="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        PreviewKeyDown="Window_PreviewKeyDown">
    <DockPanel Margin="14">
        <Button DockPanel.Dock="Bottom" Content="Tutup" Padding="16,6" Margin="0,10,0,0"
                HorizontalAlignment="Right" IsDefault="True" Click="BtnClose_Click"/>

        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <TextBlock x:Name="TxtOrderSn" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBlock x:Name="TxtResiStatus" TextWrapping="Wrap" Margin="0,0,0,4"/>
            <TextBlock x:Name="TxtPrintSummary" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock x:Name="TxtPackSummary" TextWrapping="Wrap" Foreground="#555"/>
            <TextBlock Text="Catatan order" Margin="0,10,0,4" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtOrderNotes"
                     Height="96"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Background="#FAFAFA"
                     BorderBrush="#D6D6D6"
                     Padding="8,6"/>
        </StackPanel>

        <DataGrid x:Name="LinesGrid"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPrinted}" Value="True">
                            <Setter Property="Background" Value="#E8F5E9"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsPrinted}" Value="False">
                            <Setter Property="Background" Value="#FFF3E0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="40"/>
                <DataGridTextColumn Header="Produk" Binding="{Binding ItemName}" Width="*"/>
                <DataGridTextColumn Header="Variasi" Binding="{Binding ModelName}" Width="160"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Qty}" Width="50"/>
                <DataGridTextColumn Header="Cetak produk" Binding="{Binding PrintStatusLabel}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPrinted}" Value="True">
                                    <Setter Property="Foreground" Value="#1B5E20"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsPrinted}" Value="False">
                                    <Setter Property="Foreground" Value="#E65100"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Sisi cetak" Binding="{Binding PrintSidesLabel}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
